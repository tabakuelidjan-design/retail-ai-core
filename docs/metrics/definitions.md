# Metrics — Pointer

The canonical, versioned metric definitions live in the `retail-metrics` Skill: [`.claude/skills/retail-metrics/SKILL.md`](../../.claude/skills/retail-metrics/SKILL.md).

This file exists so `docs/metrics/` isn't empty and so future ADRs can link to a stable `docs/` path; the Skill file itself is what Claude reads and what should be edited when a definition changes. If they ever diverge, the Skill file wins — fix this pointer, not the other way around.
